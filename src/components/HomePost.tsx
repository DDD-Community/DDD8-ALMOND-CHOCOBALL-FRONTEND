import React from 'react';
import './HomePost.css';

interface Props {
  src?: string;
  zIndex?: number;
  title: string;
  tagList?: string[];
}
function HomePost({ src, zIndex, title }: Props) {
  return <div></div>;
}

export default HomePost;
